# Copy this file to secrets.toml and fill in your values.
# secrets.toml is git-ignored — never commit it.

# PostGIS connection string.
# Default credentials match the docker-compose.yml service definition.
DATABASE_URL = "postgresql://datalake:datalake@localhost:35432/datalake"

# FTP server used for raw file storage.
# Remove or leave blank if FTP upload is not needed.
[ftp]
host     = "localhost"
# user     = "your_ftp_username"
# password = "your_ftp_password"
root     = "/datalake/root/"
