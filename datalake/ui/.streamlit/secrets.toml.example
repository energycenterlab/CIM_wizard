# Copy this file to secrets.toml and fill in your PostGIS connection string.
# secrets.toml is git-ignored — never commit it.

[secrets]
DATABASE_URL = "postgresql://user:password@localhost:5432/datalake"
