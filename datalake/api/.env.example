# Copy to .env and adjust for production
POSTGRES_DB=datalake
POSTGRES_USER=datalake
POSTGRES_PASSWORD=datalake
POSTGIS_PORT=35432
API_PORT=8008

# Used when running the API outside Docker (bash datawarehouse.sh)
DATABASE_URL=postgresql://datalake:datalake@localhost:35432/datalake
